server:
  port: 8080

spinnaker:
  cassandra:
    enabled: true
    embedded: false
    host: 10.0.0.153
    cluster: 'Test Cluster'
    keyspace: front50
    name: default

cassandra:
  enabled: true
  embedded: false
  host: 10.0.0.153
  cluster: 'Test Cluster'
  keyspace: front50
  name: default

redis:
  connection: redis://10.0.0.153:6379

swagger:
  enabled: true
  title: Spinnaker Front50 API
  description:
  contact:
  patterns:
    - /default/.*
    - /credentials.*
    - /global/.*
    - /notifications.*
    - /pipelines.*
    - /strategies.*
    - /v2/.*
